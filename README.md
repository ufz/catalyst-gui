# OpenGeoSys Catalyst Edition for GUI

Use the `cmake.sh`- or `cmake.bat`-script instead of a direct CMake call to configure:

```bash
mkdir build
cd build
../catalyst-gui/cmake.sh ../catalyst-gui
```
